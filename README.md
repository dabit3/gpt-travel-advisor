# GPT Travel advisor

### Requirements

Configure API keys in `.env.local`:

```
GPT_API_KEY=open-ai-api-GPT_API_KEY
```

### To run the app

1. Clone the repo

2. Install the dependencies

3. Run the app

```sh
npm run dev
```